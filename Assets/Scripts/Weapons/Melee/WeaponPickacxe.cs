public class WeaponPickacxe : MeleeWeapon
{
    public WeaponPickacxe(
        float damage,
        float attackRate,
        float range,
        int maxTarget)
        : base(damage,
            attackRate,
            range,
            maxTarget)
    {
    }
}