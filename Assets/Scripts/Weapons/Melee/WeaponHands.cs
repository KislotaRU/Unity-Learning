public class WeaponHands : MeleeWeapon
{
    public WeaponHands(
        float damage,
        float attackRate,
        float range,
        int maxTarget)
        : base(damage,
            attackRate,
            range,
            maxTarget)
    {
    }
}